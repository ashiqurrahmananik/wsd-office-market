import Link from "next/link";import {createClient} from "@/lib/supabase/server";
export async function AuthNav(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <div className="flex gap-2"><Link href="/login" className="btn btn-secondary text-sm">Sign In</Link><Link href="/signup" className="btn btn-primary text-sm">Sign Up</Link></div>;return <div className="flex items-center gap-2"><span className="hidden text-sm font-bold sm:inline">{user.user_metadata?.display_name||user.email}</span><form action="/auth/signout" method="post"><button className="btn btn-secondary text-sm">Sign Out</button></form></div>}